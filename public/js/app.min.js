    if(document.getElementById('api_secure_menu')) {
        new Vue({
            el : "#api_secure_menu",
            data : {
                menu: null,
                uri : null,
                timeInterval : null,
            },
            methods : {
                /**
                 * Metodo GET para obtener los datos
                 */
                getLoadData : function() {
                    const self = this;
                    axios.get(this.uri).then(function(response) {
                        self.menu = response.data;
                    });
                },
                /**
                 * Bucle repetitivo para la carga de datos
                 */
                setIntervalData : function() {
                    this.getLoadData();

                    const self = this;
                    setInterval(function() {
                        self.getLoadData();
                    }, this.timeInterval);
                }
            },
            created : function() {
                this.uri = document.getElementById('api_secure_menu').getAttribute('data-ajax-url');
                this.timeInterval = parseInt(document.getElementById('api_secure_menu').getAttribute('data-interval') + '000');
            },
            mounted : function() {
                this.setIntervalData();
            }
        });
    }
    if(document.getElementById('api_secure_dashboard')) {
        new Vue({
            el : "#api_secure_dashboard",
            data : {
                dashboard: null,
                uri : null,
                timeInterval : null,
            },
            methods : {
                /**
                 * Metodo GET para obtener los datos
                 */
                getLoadData : function() {
                    const self = this;
                    axios.get(this.uri).then(function(response) {
                        self.dashboard = response.data;
                    })
                    .catch(function(error) {
                        self.dashboard = null;
                        console.log(error.response.data.code, error.response.data.message);
                    });
                },
                /**
                 * Bucle repetitivo para la carga de datos
                 */
                setIntervalData : function() {
                    this.getLoadData();

                    const self = this;
                    setInterval(function() {
                        self.getLoadData();
                    }, this.timeInterval);
                }
            },
            created : function() {
                this.uri = document.getElementById('api_secure_dashboard').getAttribute('data-ajax-url');
                this.timeInterval = parseInt(document.getElementById('api_secure_dashboard').getAttribute('data-interval') + '000');
            },
            mounted : function() {
                this.setIntervalData();
            }
        });
    }
/**
 * Genera le proceso del campañas
 * a traves de viejs
 */
if(document.getElementById('api_secure_account_campaign')) {

    new Vue({

        el : "#api_secure_account_campaign",
        data : {

            dashboard: null,
            uri : null,
            timeInterval : null,
            reload : false
        },
        methods : {
            /**
             * Metodo GET para obtener los datos
             */
            getLoadData : function() {

                const self = this;

                axios.get(this.uri).then(function(response) {
                    self.reload = true;
                    self.dashboard = response.data;
                })
                    .catch(function(error) {
                        self.dashboard = null;
                        console.log(error.response.data.code, error.response.data.message);
                    });
            },
            /**
             * Bucle repetitivo para la carga de datos
             */
            setIntervalData : function() {
                this.getLoadData();

                const self = this;
                setInterval(function() {
                    self.getLoadData();
                }, this.timeInterval);
            }
        },
        created : function() {
            this.uri = document.getElementById('api_secure_account_campaign').getAttribute('data-ajax-url');
            this.timeInterval = parseInt(document.getElementById('api_secure_account_campaign').getAttribute('data-interval') + '000');
        },
        mounted : function() {
            this.setIntervalData();
        }
    });
}
/**
 * Dashbaord
 * a traves de viejs
 */
if(document.getElementById('api_secure_account_dashboard')) {

        new Vue({

            el : "#api_secure_account_dashboard",
            data : {
                dashboard: null,
                uri : null,
                timeInterval : null,
                reload : false
            },

            methods : {
                /**
                 * Metodo GET para obtener los datos
                 */
                getLoadData : function() {

                    const self = this;
                    axios.get(this.uri).then(function(response) {
                        self.reload = true;
                        self.dashboard = response.data;
                    })
                    .catch(function(error) {
                        self.dashboard = null;
                        console.log(error.response.data.code, error.response.data.message);
                    });
                },
                /**
                 * Bucle repetitivo para la carga de datos
                 */
                setIntervalData : function() {
                    this.getLoadData();

                    const self = this;
                    setInterval(function() {
                        self.getLoadData();
                    }, this.timeInterval);
                }
            },
            created : function() {
                this.uri = document.getElementById('api_secure_account_dashboard').getAttribute('data-ajax-url');
                this.timeInterval = parseInt(document.getElementById('api_secure_account_dashboard').getAttribute('data-interval') + '000');
            },
            mounted : function() {
                this.setIntervalData();
            }
        });
    }
/**
 * Genera le proceso del trades
 * a traves de viejs
 */
if(document.getElementById('api_secure_account_trade')) {

    new Vue({

        el : "#api_secure_account_trade",
        data : {
            dashboard: null,
            uri : null,
            timeInterval : null,
            reload : false
        },

        methods : {
            /**
             * Metodo GET para obtener los datos
             */
            getLoadData : function() {

                const self = this;
                axios.get(this.uri).then(function(response) {
                    self.reload = true;
                    self.dashboard = response.data;
                })
                    .catch(function(error) {
                        self.dashboard = null;
                        console.log(error.response.data.code, error.response.data.message);
                    });
            },
            /**
             * Bucle repetitivo para la carga de datos
             */
            setIntervalData : function() {
                this.getLoadData();

                const self = this;
                setInterval(function() {
                    self.getLoadData();
                }, this.timeInterval);
            }
        },
        created : function() {
            this.uri = document.getElementById('api_secure_account_trade').getAttribute('data-ajax-url');
            this.timeInterval = parseInt(document.getElementById('api_secure_account_trade').getAttribute('data-interval') + '000');
        },
        mounted : function() {
            this.setIntervalData();
        }
    });
}